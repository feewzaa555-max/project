package exception;

/**
 * ช่องในหน้าต่าง Login / Sign up
 * ใช้บอกว่าข้อความแจ้งเตือนต้องไปขึ้นใต้ช่องไหน
 */
public enum AuthField {
    USERNAME,   // ช่องชื่อผู้ใช้
    PASSWORD    // ช่องรหัสผ่าน
}